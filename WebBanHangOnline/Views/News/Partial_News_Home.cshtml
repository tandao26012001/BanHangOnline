@model IEnumerable<WebBanHangOnline.Models.EF.News>
@if (Model != null && Model.Any())
{
    @*<div class="latest-news pt-150 pb-150">
            <div class="container">

                <div class="row">
                    <div class="col-lg-8 offset-lg-2 text-center">
                        <div class="section-title">
                            <h3><span class="orange-text">Tin tức</span></h3>
                        </div>
                    </div>
                </div>

                <div class="row">
                    @foreach (var item in Model)
                    {
                        <style>
                            .news-bg {
                                background-image: url(@item.Image);
                            }
                        </style>
                        <div class="col-lg-4 col-md-6">
                            <div class="single-latest-news">
                                <a href="single-news.html">
                                    <div class="latest-news-bg news-bg">
                                    </div>
                                </a>
                                <div class="news-text-box">
                                    <h3><a href="/@item.Alias-n@(item.Id)">@item.Title</a></h3>
                                    <p class="blog-meta">
                                        <span class="author"><i class="fas fa-user"></i>@item.CreatedBy</span>
                                        <span class="date"><i class="fas fa-calendar"></i>@item.CreatedDate</span>
                                    </p>
                                    <p class="excerpt">@item.Description</p>
                                    <a href="/@item.Alias-n@(item.Id)" class="read-more-btn">Xem thêm<i class="fas fa-angle-right"></i></a>
                                </div>
                            </div>
                        </div>

                    }
                </div>
                <div class="row">
                    <div class="col-lg-12 text-center">
                        <a href="/tin-tuc" class="boxed-btn">Xem thêm</a>
                    </div>
                </div>
            </div>
        </div>*@

    <section class="latest spad">
        <div class="container">
            <div class="row">
                <div class="col-lg-12">
                    <div class="section-title">
                        <span>Tin tức mới</span>
                        <h2>Xu hướng thời trang mới</h2>
                    </div>
                </div>
            </div>
            <div class="row">
                @foreach (var item in Model)
                {
                    <div class="col-lg-4 col-md-6 col-sm-6">
                        <div class="blog__item">
                            <div class="blog__item__pic set-bg" data-setbg="@item.Image"></div>
                            <div class="blog__item__text">
                                <span><img src="/Content/assets/client/img/icon/calendar.png" alt="">@item.CreatedDate</span>
                                <h5>@item.Title</h5>
                                <a href="/@item.Alias-n@(item.Id)">Read More</a>
                            </div>
                        </div>
                    </div>
                }
            </div>
        </div>
    </section>
}
